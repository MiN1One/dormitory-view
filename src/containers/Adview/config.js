const SECTIONS = {
  noRooms: ['main', 'details', 'features','reviews', 'similar'],
  preview: ['main', 'options', 'details', 'features'],
  regular: ['main', 'options', 'details', 'features', 'reviews', 'similar']
};

const config = {
  SECTIONS,
  SCROLL_Y_OFFSET: 10
};

export default config;